name: Deploy

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Echo the message
        run: echo "Hola mundo"

      - name: Dime la fecha
        run: date

      - run: ls -l